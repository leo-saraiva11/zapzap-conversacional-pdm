package com.example.zapzap.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal - Tons de índigo/azul (estilo WhatsApp moderno)
val Primary = Color(0xFF1B5E20)           // Verde escuro
val PrimaryVariant = Color(0xFF2E7D32)    // Verde médio
val PrimaryLight = Color(0xFF4CAF50)      // Verde claro
val Secondary = Color(0xFF00BCD4)         // Ciano
val SecondaryVariant = Color(0xFF0097A7)  // Ciano escuro

// Backgrounds
val BackgroundLight = Color(0xFFF5F5F5)
val BackgroundDark = Color(0xFF121212)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1E1E1E)
val CardDark = Color(0xFF2D2D2D)

// Chat específico
val BubbleSent = Color(0xFFDCF8C6)        // Verde claro (bolha enviada)
val BubbleReceived = Color(0xFFFFFFFF)    // Branco (bolha recebida)
val BubbleSentDark = Color(0xFF056162)    // Teal escuro (bolha enviada dark)
val BubbleReceivedDark = Color(0xFF262D31) // Cinza escuro (bolha recebida dark)

// Status
val StatusOnline = Color(0xFF4CAF50)      // Verde
val StatusOffline = Color(0xFF9E9E9E)     // Cinza
val StatusBusy = Color(0xFFFF5722)        // Laranja/Vermelho

// Ticks de mensagem
val TickDefault = Color(0xFF9E9E9E)       // Cinza (enviada)
val TickDelivered = Color(0xFF9E9E9E)     // Cinza (entregue)
val TickRead = Color(0xFF4FC3F7)          // Azul claro (lida)

// Texto
val TextPrimaryLight = Color(0xFF212121)
val TextSecondaryLight = Color(0xFF757575)
val TextPrimaryDark = Color(0xFFE0E0E0)
val TextSecondaryDark = Color(0xFF9E9E9E)

// Acentos
val AccentPin = Color(0xFFFFC107)         // Amarelo (mensagem fixada)
val AccentSearch = Color(0xFFFFEB3B)      // Amarelo highlight de busca
val ErrorColor = Color(0xFFB00020)
